{
  "name": "libs",
  "version": "0.0.1",
  "author": "Meek Msaki",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:jsonrpc": "cd packages/jsonrpc && bun run build"
  },
  "dependencies": {
    "@msaki/jsonrpc": "workspace:jsonrpc"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
